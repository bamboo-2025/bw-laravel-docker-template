# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<form action="/foo/bar" method="POST">
    <input type="text" name="hitokoto">
    <input type="hidden" name="_method" value="PUT">
    <input type="hidden" name="_token" value="{{ csrf_token() }}">
</form>

### findメソッドの引数に指定しているIDは何のIDか
クリックした対象(To Do)のID(上から数えて何番目か)

### findメソッドで実行しているSQLは何か
SELECT * FROM （テーブル名） WHERE id = ??

### findメソッドで取得できる値は何か
編集対象のレコードの情報を持つTodoモデルのインスタンスを取得している。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
既存データと更新データの差分を見て判断している。
レコードがデータベースに存在しない場合は、INSERT INTOクエリが実行され、存在する場合はUPDATEクエリが実行される。

## Todo論理削除

### traitとclassの違いとは
・クラスの継承とは異なり、1つのクラスに複数のトレイとを追加することができる。
・トレイト自体はインスタンス化できない。

### traitを使用するメリットとは
・コードの再利用ができる（複数のクラスで共通の機能を再利用できる）
・クラスの継承は1つの親クラスに限定されるが、トレイトを使用することで複数のトレイトを同時に利用できる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
コントローラーのインスタンス生成時（ルート（URL）に対応するコントローラーメソッドが実行される前）「に自動で1回だけ実行される。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションを実装したかったから。
<!-- ここの答え方が合っているか分からない。 -->

### $errorsのhasメソッドの引数・返り値は何か
引数は”content”。返り値は、引数に指定したパラメータがリクエストに存在するかを判定し、結果をtrue(存在している)/false(存在していない)で返す。

### $errorsのfirstメソッドの引数・返り値は何か
引数は”content”。返り値は、条件に一致する最初のモデルインスタンス。

### フレームワークとは何か
フレームワークとは、新しいアプリケーションの開発をより効率的にする再利用可能なソフトウェアコンポーネント（特定の機能やタスクを実行する独立したソフトウェアの部品）を集めたもの。

### MVCはどういったアーキテクチャか
MVCは、コードベース内の現実世界のインスタンスまたはオブジェクトを表現した「Model(モデル)」、ユーザーがアプリケーションと対話するためのインターフェイスを表す「View(ビュー)」、ユーザーがアクションを実行すると、そのアクションを処理し、必要に応じてモデルを更新する「Controller(コントローラ)」の3つの役割に、アプリケーションを分割するアーキテクチャ（構造、設計）である。

### ORMとは何か、またLaravelが使用しているORMは何か
オブジェクト指向プログラミングで使う「オブジェクト」と、関係データベースの「テーブル」を対応付け、直接SQL文を書くことなくデータベースを操作できるプログラミング技術のこと。
またLaravelが使用しているORMは、Eloquent。

### composer.json, composer.lockとは何か
ライブラリやパッケージを管理している、プロジェクトの依存関係の管理にとって重要なファイル。

<!-- まず最初に使われるのはcomposer.json。
Composerをインストールし、composer.jsonをアプリのディレクトリに配置して、composer installをしたら、composer.jsonに書かれた情報を基に、パッケージやライブラリがvendorディレクトリ配下にまとめてインストールされる。
このとき同時に生成されるのがcomposer.lockファイル。composer install時に参照したパッケージやパッケージのバージョン情報がcomposer.lockに書き込まれる。 -->


### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか

vendorディレクトリに格納される。